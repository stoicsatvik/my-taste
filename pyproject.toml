[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-taste"
version = "0.3.0"
description = "A local-first contextual preference layer for AI agents."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Satvik"}]
dependencies = [
  "mcp[cli]>=2,<3",
  "Pillow>=10,<13",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
my-taste = "my_taste.cli:main"
my-taste-mcp = "my_taste.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/my_taste"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]


[tool.hatch.build.targets.wheel.force-include]
"skills/my-taste" = "my_taste/skill_bundle"
